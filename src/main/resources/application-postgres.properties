# MODE DEVELOPPEMENT
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# Désactive le cache de Thymeleaf pour voir les modifs en direct
spring.thymeleaf.cache=false

# Affiche plus de détails dans la console
# debug=true

# SERVEUR
server.port=8080

# BASE DE DONNEES POSTGRES
spring.datasource.url=jdbc:postgresql://localhost:5432/fdl_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
# logging.level.org.hibernate.type.descriptor.sql=OFF
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# THYMELEAF
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.mode=HTML

jwt.secret=6XRTJnygl8/qufbd2p2bmJ71ZUK0fWJ0APlL8sO8FPc=
jwt.expiration=86400000



spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=15MB
app.upload.dir=uploads